{% extends 'base.html.twig' %}

{% block body %}
    <h1>Entericacapitulo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entericaCapitulo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ entericaCapitulo.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('entericacapitulo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('entericacapitulo_edit', { 'id': entericaCapitulo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
