{% extends 'base.html.twig' %}

{% block body %}
    <h1>Entericaelemento</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entericaElemento.id }}</td>
            </tr>
            <tr>
                <th>Codigo</th>
                <td>{{ entericaElemento.codigo }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ entericaElemento.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('entericaelemento_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('entericaelemento_edit', { 'id': entericaElemento.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
