{% extends 'base.html.twig' %}

{% block body %}
    <h1>Profesionalespecialidad</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ profesionalEspecialidad.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('profesionalespecialidad_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('profesionalespecialidad_edit', { 'id': profesionalEspecialidad.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
