{% extends 'base.html.twig' %}

{% block body %}
    <h1>Religion</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ religion.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ religion.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('religion_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('religion_edit', { 'id': religion.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
