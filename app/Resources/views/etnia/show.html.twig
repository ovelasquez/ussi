{% extends 'base.html.twig' %}

{% block body %}
    <h1>Etnium</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etnium.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ etnium.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('etnia_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('etnia_edit', { 'id': etnium.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
